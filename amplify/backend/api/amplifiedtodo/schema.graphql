enum Tag {
  CLUBS
  ATHLETICS
  ACADEMICS
  VOLUNTEERING
  COMPETITIONS
}

type Activity @model @auth(rules: [{allow: public}]) {
  id: ID!
  activityName: String
  tag: Tag
  role: String
  startDate: String
  description: String
  endDate: String
}
 