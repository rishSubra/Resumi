enum Tag {
  CLUBS
  ATHLETICS
  ACADEMICS
  VOLUNTEERING
  COMPETITIONS
}

type Activity @model @auth(rules: [{allow: public}]) {
  id: ID!
  activityName: String
  startDate: String
  endDate: String
  description: String
  tag: Tag
}
 